import * as React from 'react';
import PropTypes from 'prop-types';
import Button from '../utils/Button/Button';
const Upload = () => {
    return <Button name="Upload" color="#ffae42" />;
};

Upload.propTypes = {};

export default Upload;
