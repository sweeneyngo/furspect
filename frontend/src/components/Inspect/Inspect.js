import * as React from 'react';
import PropTypes from 'prop-types';
import Button from '../utils/Button/Button';
const Inspect = () => {
    return <Button name="Inspect" color="#FFCF07" />;
};

Inspect.propTypes = {};

export default Inspect;
